[package]
name = "rustdv-uvm"
version = "0.1.0"
edition = "2021"
description = "The UVM analog: component lifecycle, objections, channels, analysis broadcast, TLM FIFOs, and the sequencer handshake (design-doc §5)."
license = "MIT OR Apache-2.0"

[lib]
# Crate root named after the crate, not lib.rs (D29).
path = "src/rustdv_uvm.rs"

[dependencies]
rustdv-sim = { path = "../rustdv-sim" }
