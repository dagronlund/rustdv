[package]
name = "rustdv-sim"
version = "0.1.0"
edition = "2021"
description = "The cocotb-analog core: simulator-driven async executor, tasks, triggers, typed handles, clock, sim-aware queues (design-doc §4)."
license = "MIT OR Apache-2.0"

[dependencies]
rustdv-gpi = { path = "../rustdv-gpi" }
