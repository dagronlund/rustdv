[package]
name = "ch32_analysis_ports"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
# Crate root named after the crate, not lib.rs (D29).
path = "src/ch32_analysis_ports.rs"

[dependencies]
rustdv = { path = "../../../rustdv" }
tinyalu_utils = { path = "../tinyalu-utils" }
