[package]
name = "ch29_factory"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]
# Crate root named after the crate, not lib.rs (D29).
path = "src/ch29_factory.rs"

[dependencies]
rustdv = { path = "../../../rustdv" }
