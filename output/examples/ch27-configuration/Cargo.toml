[package]
name = "ch27_configuration"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
# Crate root named after the crate, not lib.rs (D29).
path = "src/ch27_configuration.rs"

[dependencies]
rustdv = { path = "../../../rustdv" }
