[package]
name = "ch25_uvm_env_testbench_4_0"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]
# Crate root named after the crate, not lib.rs (D29).
path = "src/ch25_uvm_env_testbench_4_0.rs"

[dependencies]
rustdv = { path = "../../../rustdv" }
tinyalu_utils = { path = "../tinyalu-utils" }
