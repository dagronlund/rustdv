[package]
name = "ch24_components"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]
# Crate root named after the crate, not lib.rs (D29).
path = "src/ch24_components.rs"

[dependencies]
rustdv = { path = "../../../rustdv" }
