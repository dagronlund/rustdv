[package]
name = "tinyalu_utils"
version = "0.1.0"
edition = "2021"

[lib]
# Crate root named after the crate, not lib.rs (D29).
path = "src/tinyalu_utils.rs"

[dependencies]
rustdv = { path = "../../../rustdv" }
