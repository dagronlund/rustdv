# Project: Rust for RTL Verification

## Context
- I am the author of "Python for RTL Verification" (source PDF in /reference).
  Treat it as ground truth on the Python/cocotb side.
- I do not know Rust. This is my learning project — optimize for teaching,
  not just deliverables.

## Folder structure
- /reference — read-only. cocotb-src, pyuvm-src, book PDF, SOURCES.md.
  Do not modify anything here.
- /output — write generated deliverables here only.

## Scope for this session
- Design doc + book outline (and Ch. 1 draft) ONLY.
- Do NOT write Rust implementation code. Code generation is a separate
  phase with a different model.

## Standing rules
- Always ask before overwriting an existing file in /output.
- Cite which source (cocotb, pyuvm, or the book) informed each design
  decision in the design doc.
- Flag any place where you're uncertain or guessing — add it to an
  "Open Questions" section rather than presenting it as settled.
- Show a short plan before generating long documents; confirm before
  proceeding.